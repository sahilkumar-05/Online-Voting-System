@model IEnumerable<ONLINEVS_Project.Models.Candidate>

@{
    ViewData["Title"] = "Candidates";
}

<div class="container py-4">

    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1 class="fw-bold">Candidates List</h1>
        <div>
            <a asp-controller="Admin" asp-action="Dashboard" class="btn btn-secondary me-2">
                <i class="bi bi-house-door-fill"></i> Back to Dashboard
            </a>
            <a asp-action="Create" class="btn btn-primary">
                <i class="bi bi-plus-circle"></i> Create New
            </a>
        </div>
    </div>

    <div class="card shadow-sm">
        <div class="card-body p-0">
            <table class="table table-hover align-middle mb-0">
                <thead class="table-light">
                    <tr>
                        <th>@Html.DisplayNameFor(model => model.Candidate_Cnic)</th>
                        <th>@Html.DisplayNameFor(model => model.Name)</th>
                        <th>@Html.DisplayNameFor(model => model.Party)</th>
                        <th>@Html.DisplayNameFor(model => model.Election)</th>
                        <th class="text-center">Actions</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>@Html.DisplayFor(modelItem => item.Candidate_Cnic)</td>
                            <td>@Html.DisplayFor(modelItem => item.Name)</td>
                            <td>@Html.DisplayFor(modelItem => item.Party)</td>
                            <td>@Html.DisplayFor(modelItem => item.Election.Description)</td>
                            <td class="text-center">
                                <a asp-action="Edit" asp-route-id="@item.Candidate_ID" class="btn btn-sm btn-warning me-1">
                                    <i class="bi bi-pencil-square"></i> Edit
                                </a>
                                <a asp-action="Details" asp-route-id="@item.Candidate_ID" class="btn btn-sm btn-info me-1">
                                    <i class="bi bi-eye"></i> View
                                </a>
                                <a asp-action="Delete" asp-route-id="@item.Candidate_ID" class="btn btn-sm btn-danger">
                                    <i class="bi bi-trash"></i> Delete
                                </a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>

</div>

<style>
    .table-hover tbody tr:hover {
        background-color: #f1f1f1;
    }

    .btn i {
        margin-right: 4px;
    }
</style>
